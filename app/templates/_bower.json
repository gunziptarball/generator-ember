{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {
    "jquery": "~1.9.1",
    "bootstrap": "~3.0.0"
  },
  "resolutions": {
  },
  "devDependencies": {<% if (testFramework === 'mocha') { %>
    "ember-mocha-adapter": "0.1.1"<% } %>
  }
}
